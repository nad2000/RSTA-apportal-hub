:
export ENV=${1:-local}
[ -d ./venv ] && source ./venv/bin/activate || workon venv

if [ -d "$HOME/$ENV" ] ; then
	python $HOME/$ENV/manage.py process_tasks
else
	python ./manage.py process_tasks
fi
